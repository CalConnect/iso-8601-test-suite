# TODO.cleanup/23-remove-stale-src-matrix-json

**Status:** DONE

`site/src/matrix.json` (10.5MB, dated May 29) is a stale copy of the matrix data.
The application fetches `/matrix.json` from `site/public/matrix.json` (the regenerated
version from `scripts/capability-matrix -o site/public/matrix.json`). The src copy is
not imported or referenced anywhere — it's dead weight inflating the repository.

## Fix applied

Removed the stale `site/src/matrix.json`. The authoritative copy lives at
`site/public/matrix.json` and is regenerated by the capability-matrix script.
