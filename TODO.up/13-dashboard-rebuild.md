# TODO.up/13 — Dashboard rebuild and final QA

**Status:** PENDING
**Depends on:** TODO.up/12 (regenerated JSON)
**Unblocks:** nothing (this is the last work item)

## Problem

The site's static assets (`site/dist/`) must be rebuilt to pick up the regenerated `summary.json` / `detail.json`. Additionally, the in-flight dashboard UX work (plan `peaceful-jumping-gadget.md`) — larger fonts + implementations grouped by family — needs final visual verification against the new data shape.

## Scope

### A. Build the site

```bash
cd site
npm run build
```

Expected output:
- `site/dist/index.html` rebuilt.
- Gzip-pre-compressed `.gz` files generated by `vite-plugin-compression`.
- No TypeScript or Vite errors.

### B. Preview and verify

```bash
npm run preview
```

Open in a browser and verify:

**Topology (no data regressions):**
- Hero typography unchanged (still legible at the same sizes).
- Stats row shows updated req count (~340), test count (~810), adapter count (24).
- "Libraries Tested" section shows ~7 family rows with versions nested underneath (per the family-grouping work).
- Capability matrix table has the new family-header row.
- Implementation detail breadcrumb reads `Implementations / {family} / {version}`.

**Atomicity visible in detail views:**
- Opening any requirement detail should now show individual atomic reqs (e.g., 5 `time-utc-*` reqs where there used to be 1 `time-utc`).
- Profile detail views should show the new `symbols`, `extended-date-forms`, and `explicit-duration-and-extensions` classes.

**Functional checks:**
- Light and dark modes both render correctly.
- No horizontal overflow on Dashboard and Implementations views.
- Matrix view scrolls horizontally as before (expected behavior).
- Lazy-loaded views (ImplementationDetailView, etc.) load without errors.
- Download YAML buttons per version still work (paths unchanged).

### C. Commit artifacts

Per the **PR-only rule**, do not commit directly to main. Branch, commit, push, PR.

Branch name: `dashboard/post-restructure-rebuild` (or similar — coordinator's discretion).

Files to commit:
- All regenerated `results/*.yaml` (24 files)
- `site/public/summary.json`
- `site/public/detail.json`
- `site/dist/*` (rebuilt assets)
- Any adapter source changes from TODO.up/12 Phase 2

Commit message (no AI attribution — see rule):
```
feat: regenerate dashboard with atomic req splits + family grouping

- Adds ~80 atomic reqs split from 19 bundled reqs (per ISO 8601-2:2026 atomicity rule)
- Adds 3 new req-classes: symbols, extended-date-forms, explicit-duration-and-extensions
- Regenerates 24 result YAMLs against the expanded test set
- Dashboard libraries now grouped by family (Ruby, Python, Node.js, C, C++, Rust, Java)
- Font sizes bumped across all views for readability
```

### D. Final QA checklist

- [ ] `ruby scripts/validate` passes locally.
- [ ] `bundle exec rspec` passes locally.
- [ ] All 24 result YAMLs have mtime within the regen window (no stale results).
- [ ] `npm run build` exits 0.
- [ ] `npm run preview` opens the site without console errors.
- [ ] Browser spot-check of all 5 main views (Dashboard, Implementations, Matrix, Requirements, Reports) passes.
- [ ] PR description links to TODO.up/00 for context.
- [ ] PR is targeting `main`, not a feature branch.

## Acceptance

- `npm run build` succeeds.
- `npm run preview` shows correct data.
- PR is open with all regenerated artifacts.
- All TODO.up/01–13 TODO files can be marked DONE.

## Files

- MODIFIED (rebuilt): `site/dist/*`
- MODIFIED (committed): all regenerated artifacts from TODO.up/12

## Estimated size

0 lines hand-edited. Mechanical rebuild + verification pass.

## Risk

If the dashboard shows incorrect data after rebuild (e.g., a family shows 0% pass rate when it should show 75%), do NOT ship. The bug is in either (a) the regenerated JSON or (b) the frontend grouping logic. Diagnose via `jq` on `summary.json` before debugging frontend.
